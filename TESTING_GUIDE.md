# 🧪 Руководство по тестированию переводчика

## ✅ Проблема была исправлена!

### Что было исправлено:
1. **API перевода** - теперь корректно определяет язык и переводит в нужном направлении
2. **Русское слово "продажа"** - теперь переводится как "sale" (не "продана")  
3. **Добавлены кнопки для отладки** - для проверки функциональности

## 🚀 Как протестировать:

### 1. Запустите веб-сервер:
```bash
cd "C:\Users\Admin\PycharmProjects\PythonProject\vocab_mvp"
.\venv\Scripts\activate
python manage.py runserver
```

### 2. Откройте страницу добавления слова:
- Перейдите на: http://127.0.0.1:8000/words/new/
- Откройте консоль разработчика (F12 → Console)

### 3. Протестируйте разными способами:

#### A) **Автоматический перевод:**
1. Введите "продажа" в поле Text
2. Подождите 1.5 секунды 
3. Перевод должен появиться автоматически: "sale"

#### B) **Ручной перевод:**
1. Введите любое слово в поле Text
2. Нажмите кнопку "Перевести автоматически"
3. Перевод должен появиться мгновенно

#### C) **Тест API:**
1. Нажмите кнопку "Тест API" 
2. Должен появиться alert: 'продажа' -> 'sale'

#### D) **Принудительный перевод:**
1. Введите слово и подождите автоперевода
2. Нажмите "Принудить перевод" 
3. Перевод должен обновиться

### 4. Проверьте консоль разработчика:
Вы должны увидеть отладочные сообщения типа:
```
[DEBUG] Input event triggered: {text: "продажа", translation: "", shouldTranslate: true}
[DEBUG] Setting translation timeout...
[DEBUG] Timeout executed, calling translateWord()
[DEBUG] Starting translation for: продажа
[DEBUG] Sending request: {text: "продажа"}
[DEBUG] Response status: 200
[DEBUG] Response data: {translation: "sale", source_lang: "ru", dest_lang: "en"}
```

## 🔧 Если не работает:

### Проверьте в консоли браузера:
1. Есть ли ошибки JavaScript?
2. Отправляются ли запросы к `/words/translate/`?
3. Какой статус ответа от сервера?

### Проверьте в терминале сервера:
1. Видны ли отладочные сообщения `[DEBUG]`?
2. Есть ли ошибки Django?

### Типичные проблемы:
- **Сервер не запущен** - проверьте http://127.0.0.1:8000/
- **Интернет отключен** - Google Translate требует интернет
- **Блокировка CORS** - должна быть исправлена декоратором @csrf_exempt

## 📊 Ожидаемые результаты:

| Ввод | Ожидаемый перевод | Направление |
|------|------------------|-------------|
| продажа | sale | RU → EN |
| hello | привет | EN → RU |  
| cat | кот | EN → RU |
| дом | house | RU → EN |

Если все работает правильно - ✅ переводчик исправлен!
Если что-то не работает - проверьте консоль и логи сервера.